<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.example.lesson04.dao.StudentDAO">
 	<insert id="insertStudent" parameterType="com.example.lesson04.model.Student"
 	useGeneratedKeys="true" keyProperty="id"> <!-- 자동으로 id가져오기 -->
 		INSERT INTO `new_student`
 		(
 			`name`
			,`phoneNumber`
			,`email`
			,`dreamJob`
			,`createdAt`
			,`updatedAt`
 		)
 		VALUE
 		(
 			#{name}
 			,#{phoneNumber}
 			,#{email}
 			,#{dreamJob}
 			,NOW()
 			,NOW()
 		)
 	
 	</insert>
 	
 	<select id="selectStudentById" 
 	parameterType="int" resultType="com.example.lesson04.model.Student">
 		SELECT
 			`id` 
 			,`name`
			,`phoneNumber`
			,`email`
			,`dreamJob`
			,`createdAt`
			,`updatedAt`	
 		FROM
 			`new_student`
 		WHERE
 			`id` = #{id}
 	</select>
 </mapper>