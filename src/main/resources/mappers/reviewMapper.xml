<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
 
 <mapper namespace="com.example.lesson03.dao.ReviewDAO">
 	<select id="selectReview" 
 	parameterType="int"
 	resultType="com.example.lesson03.model.Review"> <!-- 무조건 reusltType은 select 문에서만 있음 -->
 		SELECT
	 		`id`  
			,`storeId`  
			,`menu` 
			,`userName`  
			,`point` 
			,`review` 
			,`createdAt` 
			,`updatedAt` 
 		FROM
 			`new_review`
 		WHERE
 			`id` = #{id}	
 	
 	</select>
 	
 	<insert id="insertReview" parameterType="com.example.lesson03.model.Review">
 		INSERT INTO `new_review`
 		(
 			`storeId`  
			,`menu` 
			,`userName`  
			,`point` 
			,`review` 
			,`createdAt` 
			,`updatedAt` 
 		)
 		VALUES <!-- getter 를 사용하여 수행됨 -->
 		(
 			#{storeId}
 			, #{menu}
 			, #{userName}
 			, #{point}
 			, #{review}
 			, NOW()
 			, NOW()
 		)
 	</insert>
 
 </mapper>